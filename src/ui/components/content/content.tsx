import * as React from "react";
import { makeStyles } from "@material-ui/core/styles";

const ContentPage = () => {
  return (
    <div
      style={{
        gridArea: "main",
      }}
    >
      这是首页
    </div>
  );
};

export default ContentPage;
